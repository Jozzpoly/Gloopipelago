# M1.2 Browser Delivery — Candidate Report

**Date:** 2026-09-09  
**Status:** **CANDIDATE READY / OWNER BROWSER SMOKE REQUIRED / NOT PROMOTED**

## Scope

M1.2 is limited to generated standalone browser delivery and product smoke. It does not authorize M2+, RNG stream splitting, lineage, logical-world semantics, scheduler cleanup, new heredity/ecology, or other substrate work.

## Generated delivery

The candidate is generated by `tools/m12.mjs` into local build output `dist/gloopipelago.html`. `dist/` is intentionally not committed; the artifact is reproducible output, not repository source.

Generation uses the frozen V1 HTML as the product shell/template and mechanically replaces only the historical embedded simulation-core region with the current maintained `src/core/v1-mirror.mjs`. The generated HTML is an output specimen, not a second hand-maintained source of biology. The exact candidate supplied for Owner smoke is identified by its SHA-256 in the build receipt.

Build receipt: `evidence/m1/m1_2_build_receipt.json`.

Qualified candidate:

- output SHA-256: `9750e602ae61d712c64b046f130dac4c743e718d45c638ff423d5484aed9a5b0`;
- output bytes: `18271`;
- frozen template SHA-256 remains `ededf979b857f795a93f8d019ab3fc6364df0156885f381050641ab30ffff1d8`;
- maintained-core SHA-256 is `77c5736b56a2c85799df8caaa221d85cb583c1ef63c1533b74102f73f86efd7b`;
- frozen prefix: byte-identical;
- frozen suffix/UI/render/control shell: byte-identical;
- injected maintained core: byte-identical to `src/core/v1-mirror.mjs`;
- no external script dependency;
- inline ES-module syntax check: PASS;
- repeated generation is deterministic and `verify` rejects stale/hand-edited dist output.

## Product contract smoke

`node tools/m12.mjs dom-smoke` executes the generated inline module with a deliberately small DOM/canvas contract stub. This is **not browser evidence**; it only checks integration wiring that static syntax cannot catch.

Current receipt: `evidence/m1/m1_2_node_dom_smoke.json`.

PASS:

- world boots with a non-empty population and food;
- draw path executes;
- 4× speed control handler executes;
- pause/resume executes;
- +50 food executes and is reflected in UI;
- pointer food interaction executes;
- catastrophe, same-seed reset and new-seed reset execute.

Because the V1 shell outside the injected core is byte-identical, the original 1× default control, layout, renderer and non-dashboard product surface are preserved structurally.

## Causal regression

After adding M1.2 build tooling and generated output:

- M1.1 full Oracle↔maintained-core differential: **8/8 PASS**;
- protected process specimens: **PASS**;
- M0 regression: **8/8 PASS**;
- state-diff self-test: **PASS**.

No causal-core change was introduced by M1.2.

## Browser limitation

A fresh M1.2 minimal Chromium sanity probe timed out before any Gloop product was loaded. This reproduces the M0 environment limitation; see `evidence/m1/m1_2_browser_environment.json`.

Therefore **no real-browser PASS is claimed from the container**. Node contract smoke must not be substituted for this gate.

## Required Owner smoke

Promotion remains blocked until the exact candidate `dist/gloopipelago.html` is opened in a normal desktop browser and the Owner confirms:

1. it opens directly as a standalone one-file artifact and visibly runs;
2. the living world looks and behaves recognizably like historical V1;
3. pause/resume works;
4. 1× and 4× speed are both usable;
5. +50 food, catastrophe, same-seed reset and new-seed reset work;
6. clicking/tapping the world adds local food as expected;
7. resize does not produce an obvious product failure beyond known V1 semantics;
8. there is no new permanent research-dashboard wall or obvious experience regression.

Any material failure blocks promotion and returns M1.2 to repair/retest. A clean Owner smoke can promote this candidate without authorizing M2 implementation automatically beyond the next explicitly reviewed project boundary.
