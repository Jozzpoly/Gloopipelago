# M1.2 Standalone Browser Delivery — Qualification Report

**Date:** 2026-09-09  
**Status:** **PASS / READY FOR PROMOTION**

## Scope

M1.2 qualifies generated standalone browser delivery only. It does not change simulation biology and does not authorize M2 implementation, RNG stream splitting, lineage, logical-world semantics, scheduler cleanup, new heredity/ecology, or other substrate expansion.

## Candidate identity

The Owner-tested artifact was generated by `tools/m12.mjs` from the frozen V1 browser shell plus the maintained `src/core/v1-mirror.mjs`. `dist/` remains untracked generated output rather than a second maintained source.

- candidate commit before promotion: `43e89f300536bc1a5abb0655a556c0bef9f8f558`
- generated artifact SHA-256: `9750e602ae61d712c64b046f130dac4c743e718d45c638ff423d5484aed9a5b0`
- output bytes: `18271`
- frozen V1 template SHA-256: `ededf979b857f795a93f8d019ab3fc6364df0156885f381050641ab30ffff1d8`
- maintained core SHA-256: `77c5736b56a2c85799df8caaa221d85cb583c1ef63c1533b74102f73f86efd7b`

## Automated evidence

PASS before Owner smoke:

- frozen V1 prefix and suffix/UI shell are byte-identical in the generated artifact;
- maintained core injection is byte-identical;
- deterministic rebuild and stale/hand-edit rejection pass;
- inline ES-module syntax passes;
- artifact has no external script dependency;
- Node DOM/canvas contract smoke passes world boot, draw path, pause/resume, 4×, +50 food, pointer food, catastrophe, reset and new seed;
- M1.1 causal regression remains 8/8 exact with protected process specimens;
- M0 Oracle regression remains 8/8 exact with state-diff self-test.

The container Chromium sanity route remains unavailable before product load, so it is not counted as browser evidence.

## Owner real-browser evidence

Owner opened the exact candidate in a normal desktop browser and supplied a ~142.8 s recording. Owner assessment was that the interface is raw but the candidate appears to work. Review of the recording found no blocking visible crash, freeze, or new product regression.

Key product evidence:

- requested 4× is actually sustained over a measured 90 s recording interval: world time advances from 68 s to 428 s, i.e. +360 simulated seconds / +90 real seconds = 4.00×;
- the living world remains active through a >400 simulated-second run, including population near 193 and large resource/population pulses;
- rapid New Seed scouting remains usable at persistent 4× and emerges as a real Owner workflow;
- generated delivery remains recognizably the V1 product surface rather than a new dashboard.

The recording does not constitute console-error evidence and does not meaningfully stress resize. These are recorded caveats, not hidden claims.

Detailed qualitative analysis is preserved in `M1_2_OWNER_SMOKE_VIDEO_ANALYSIS.md`.

## Non-blocking debts discovered by Owner smoke

The smoke exposed useful later requirements without broadening M1.2:

- sidebar truncates the 32-bit seed to six digits, which is inadequate provenance for seed scouting;
- `max generacja` is highest currently living generation, not historical maximum;
- UI `generalista` threshold is an interpretive classifier, not functional both-food generalism;
- mature/high-sense populations produce heavy sense-ring visual clutter;
- Owner strongly wants x10/x20+ for long-horizon stress/scouting, but this should be designed as a real fast-forward/stress mode with achieved-speed reporting rather than merely extending the current slider.

These are post-M1 product/research debts, not M1.2 failures.

## Promotion verdict

**M1.2 PASS.** The previously missing real-browser/Owner gate is cleared. The candidate may be promoted to `main` without changing the tested generated artifact or widening M1 scope.

After promotion, the project should perform a short post-M1 integration/boundary review before authorizing M2 implementation.
