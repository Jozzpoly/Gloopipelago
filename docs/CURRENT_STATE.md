# Gloopipelago — Current State

**Date:** 2026-09-10  
**State:** **M0 PROMOTED / M1 COMPLETE & PROMOTED / B0 PROMOTED / M2a PROMOTED / POST-M2a BOUNDARY COMPLETE / M3 CONTRACT QUALIFIED FOR PROMOTION / UPON MERGE M3 IMPLEMENTATION AUTHORIZED / M2b DEFERRED.**

## Project identity

Gloopipelago is a **living evolution observatory**: the living world is primary; causal apparatus is enabling infrastructure.

## Canonical repository

`Jozzpoly/Gloopipelago`

`main` is implementation authority. Every bounded implementation slice branches from live `main` after the previous authority boundary is reconciled.

## Frozen historical authority

`archive/v1/gloopipelago_single.html`  
SHA-256 `ededf979b857f795a93f8d019ab3fc6364df0156885f381050641ab30ffff1d8`

The frozen artifact is immutable provenance. It is no longer intended to be an active live-product source after B0.

## M0 / M1

**PROMOTED.**

M0 established executable historical truth. M1 established the maintained causal core, explicit V1-compatible RNG state, deterministic standalone delivery and positive Owner real-browser evidence.

Exact Owner-tested M1.2 artifact:
SHA-256 `9750e602ae61d712c64b046f130dac4c743e718d45c638ff423d5484aed9a5b0`
(18271 bytes).

## Post-M1 / B0

Post-M1 review selected Browser Surface Ownership Mirror before M2. B0 is **PASS / PROMOTED**.

Live standalone browser source is maintained under `src/browser/*`; the frozen archive is no longer a live build input. See `docs/POST_M1_BOUNDARY_REVIEW_2026-09-09.md` and `evidence/b0/B0_QUALIFICATION_REPORT.md`.

## M2a — Passive Lifecycle Witness

**PASS / PROMOTED.**

Detached downstream-only founder/birth/death witness records are qualified without changing RNG, scheduler, world, browser-controller or biological semantics. `Simulation` retains no lifecycle history.

Promoted core SHA-256:
`18e7ed7dea643008eeaa7e0c037708a23e9fcdf0c78d1dcb825aa5bc9a497c56`

See `evidence/m2a/M2A_QUALIFICATION_REPORT.md`.

## Post-M2a boundary

**COMPLETE / PROMOTED.**

See `docs/POST_M2A_BOUNDARY_REVIEW_2026-09-10.md`.

Key decision:

- M2b retention/history is deferred;
- browser viewport authority is a demonstrated causal/reproducibility confound;
- M3 Logical World / View Separation became the next Integration-Spine subject;
- contract/geometry qualification had to precede runtime implementation.

## M3 contract — Logical World / View Separation

**QUALIFIED FOR AUTHORITY PROMOTION.**

Contract: `docs/M3_LOGICAL_WORLD_VIEW_SEPARATION_CONTRACT_2026-09-10.md`.

Geometry authority: `docs/M3_GEOMETRY_AUTHORITY_RECEIPT_2026-09-10.md`.

Geometry evidence: `evidence/m3/M3_GEOMETRY_FEASIBILITY_REPORT.md` and reproducible `tools/m3-geometry.mjs`.

### Canonical logical world

`900x700` is selected for the first M3 browser epoch.

This is an authority/provenance decision, not a claim of optimal ecology. The exact-source 21-seed x 3-geometry campaign demonstrates that geometry materially changes trajectories.

Exact-source geometry receipt:

- GitHub Actions run `34421980063`: **completed / success**;
- exact promoted M2a core asserted;
- 4/4 M0 full-state + RNG reference validations PASS;
- 63 novel geometry rows;
- 63/63 lifecycle accounting PASS;
- artifact id `10131234964`;
- extracted result JSON SHA-256 `7b59981c4190789b2ff77a0e3b664e276bd3e461d99fcbe422e1098ae6dbb2b5`.

### M3 implementation contract

After this contract PR is merged, a fresh implementation branch is authorized to do only the bounded world/view separation:

- construct browser worlds at logical 900x700;
- browser resize changes view state only and never calls `sim.setSize()`;
- render through uniform aspect-preserving contain;
- letterbox/pillarbox space has no world authority;
- pointer interventions map view -> logical coordinates and ignore letterbox clicks;
- preserve explicit `Simulation.setSize()` as intentional world-semantic apparatus;
- preserve M2a/M1.1/M0 core semantics and historical profiles.

M3 promotion later requires exact viewport-invariance, initial-viewport-independence, pointer-mapping, rendering-transform, protected-core, performance and real-browser Owner gates.

## Next bounded action

**Promote the qualified M3 contract, then create a fresh bounded M3 implementation branch from the resulting live `main`.**

Implementation must not begin from the pre-promotion contract branch.

## Later evidence-backed needs

Still deliberately outside M3:

- full/copyable seed and later recent/bookmark workflow;
- precise living-vs-historical generation labels;
- functional-vs-UI diet classification;
- optional sense-ring visibility;
- true x10/x20+ fast-forward/stress mode with achieved-speed reporting;
- retained lineage/history only if a concrete workflow demonstrates the need.

## Negative decisions

Do not edit frozen V1. Do not clean up Accidental Biology by inertia. Do not reintroduce viewport-driven world sizing. Do not infer logical geometry from recording resolution. Do not normalize biology to area inside M3. Do not delete explicit `Simulation.setSize()` merely because browser resize loses authority. Do not bundle M2b, fast-forward, seed UX, classifier wording, scheduler cleanup, RNG splitting or new biology into M3.
