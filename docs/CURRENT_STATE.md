# Gloopipelago — Current State

**Date:** 2026-09-10  
**State:** **M0 PROMOTED / M1 PROMOTED / B0 PROMOTED / M2a PROMOTED / M3 CONTRACT PROMOTED / M3 DPR REPAIR MACHINE PASS / x40 MACHINE PASS / ONE COMPOSITE OWNER SMOKE REQUIRED / M3 + x40 NOT YET PROMOTED / M2b DEFERRED.**

## Project identity

Gloopipelago is a **living evolution observatory**: the living world is primary; causal apparatus is enabling infrastructure.

## Canonical repository

`Jozzpoly/Gloopipelago`

`main` remains promoted implementation authority. Qualified branch state must not be described as live authority until promotion is complete.

## Frozen historical authority

`archive/v1/gloopipelago_single.html`  
SHA-256 `ededf979b857f795a93f8d019ab3fc6364df0156885f381050641ab30ffff1d8`

The frozen artifact remains immutable provenance.

## Promoted foundation

### M0 / M1 / B0

**PROMOTED.**

M0 established executable historical truth. M1 established the maintained causal core, explicit V1-compatible RNG state, deterministic standalone delivery and positive Owner browser evidence. B0 moved the live browser surface into maintained source without changing the qualified product.

Historical M1.2 Owner-tested standalone:

- SHA-256 `9750e602ae61d712c64b046f130dac4c743e718d45c638ff423d5484aed9a5b0`;
- 18,271 bytes.

### M2a — Passive Lifecycle Witness

**PROMOTED.**

Detached downstream-only founder/birth/death witness records remain qualified without changing RNG, scheduler, world or biological semantics. `Simulation` retains no lifecycle history.

Promoted maintained-core SHA-256:

`18e7ed7dea643008eeaa7e0c037708a23e9fcdf0c78d1dcb825aa5bc9a497c56`

M2b retained history remains deferred.

## M3 — Logical World / View Separation

### Contract

**PROMOTED CONTRACT; IMPLEMENTATION NOT YET PROMOTED.**

M3 defines:

- canonical browser logical world **900x700**;
- CSS viewport and DPR as presentation-only state;
- centered uniform aspect-preserving `contain`;
- letterbox/pillarbox with no world authority;
- inverse pointer mapping into logical coordinates;
- explicit `Simulation.setSize()` retained as intentional world-semantic apparatus;
- no biology/RNG semantic changes.

Canonical contract/evidence:

- `docs/POST_M2A_BOUNDARY_REVIEW_2026-09-10.md`;
- `docs/M3_LOGICAL_WORLD_VIEW_SEPARATION_CONTRACT_2026-09-10.md`;
- `docs/M3_GEOMETRY_AUTHORITY_RECEIPT_2026-09-10.md`;
- `evidence/m3/M3_GEOMETRY_FEASIBILITY_REPORT.md`.

### Skeptical audit correction

The original M3 machine campaign produced valid evidence for the paths it actually exercised, but a later skeptical audit found a real omitted contract case: pure DPR-only presentation change.

Real Chrome showed that the original product did not refresh the canvas backing buffer when DPR changed without a CSS resize. Therefore the old blanket **M3 IMPLEMENTATION MACHINE PASS** claim was superseded rather than retroactively rewritten.

The audit also established that:

- the original paused performance sentinel did not itself prove achieved simulation speed;
- a later Owner protocol had incorrectly weakened the required narrow/mobile-like M3 smoke;
- qualification pipelines must preserve failing process exit codes;
- the old RAF-coupled scheduler could not honestly provide 40x and accumulated historical catch-up debt.

### Repaired M3 machine authority

**REPAIRED MACHINE PASS / OWNER PRODUCT GATE STILL OPEN.**

Branch/PR:

- `m3-logical-world-view-implementation`;
- PR `#11` — draft, unmerged.

Exact repaired runtime qualification head:

`1411f46831aad00e579c6cf03f36b7fcef1b1001`

Exact repaired standalone:

- SHA-256 `fb568d1b4fb5f4ed3e584bbab4662ad19339851efc78756c97e242283600e43f`;
- 22,860 bytes;
- app SHA-256 `426ceafc5e7cf1c287a08e62dabef361e8d527c768aae22e768511bfd5637f5a`;
- core SHA-256 `18e7ed7dea643008eeaa7e0c037708a23e9fcdf0c78d1dcb825aa5bc9a497c56`.

Repair behavior: each active presentation frame detects DPR mismatch and invokes the existing presentation-only resize path. DPR gains no causal authority.

Final M3 repair qualification:

- Actions run `34429956481`: **success**;
- Chrome `152.0.7977.82`;
- artifact id `10134150422`;
- artifact digest `sha256:9adc833e465f0254f834de0e0a8a6c5a1490fd856414258b37cdc6a48784aef5`.

Real-browser gates PASS:

- wide -> narrow -> wide authoritative-state invariance while paused;
- pure DPR 1 -> 2 at fixed CSS `390x408`, backing buffer `390x408 -> 780x816`, authoritative state exact;
- letterbox pointer no-op;
- in-world pointer exactly +18 food;
- achieved 4x approximately `3.994x`;
- no page errors;
- protected M0/M1.1/M2a chain PASS.

The first `matchMedia` repair attempt is preserved as failed run `34429727210`; it was not treated as flaky or hidden.

See `evidence/m3/M3_DPR_REPAIR_MACHINE_QUALIFICATION_2026-09-10.md`.

## Owner M3 evidence

The earlier ~4m47s Owner recording remains useful partial positive evidence for wide presentation, controls, pointer boundaries and sustained 4x.

It does **not** complete the promoted contract's materially narrow/mobile-like product-feel check, substantial live wide -> narrow/tall -> wide continuity, or seed restart checks after resize.

Those requirements remain mandatory and will be completed on the composite x40 candidate to avoid spending Owner attention on another obsolete pre-fast-forward build.

See `evidence/m3/M3_OWNER_SMOKE_PARTIAL_2026-09-10.md`.

## Honest x40 fast-forward

### Contract

**CONTRACT WRITTEN BEFORE IMPLEMENTATION RESULTS.**

See:

`docs/X40_HONEST_FAST_FORWARD_CONTRACT_2026-09-10.md`

The scheduler must:

- preserve exact sequential `Simulation.step(1/60)`;
- decouple simulation stepping from display refresh;
- keep target speed and achieved speed separate;
- bound main-thread CPU slices to 6 ms;
- cap retained simulation backlog at 0.25 s;
- clear historical timing debt across speed/pause/reset/visibility transitions;
- create no hidden-tab evolution or return-time catch-up;
- keep high-speed render cadence independent from simulation cadence;
- report underachievement instead of falsifying 40x;
- avoid Worker/core/biology changes unless evidence requires them.

### Machine-qualified x40 runtime

**MACHINE PASS / OWNER PRODUCT GATE OPEN / NOT PROMOTED.**

Stacked branch:

`x40-honest-fast-forward-2026-09-10`

Dependency base:

repaired M3 branch head `ccf1186ab65b3623fa6ef6ce4abf90a483b2e2d0`.

Exact machine-qualified x40 runtime head:

`2081e4a1462a52fe20b4c3f8a60ef54a968977ed`

Exact composite standalone:

- SHA-256 `21bb4518c3cdf2557ebeb008d9cb9bbfee1ff3e45ca06734d535106db6438b46`;
- 26,912 bytes;
- app SHA-256 `ade461fee63fe5a48aecb6eb967e75b437a136a09728f0036cbf1ff9e14b4d4b`;
- core remains exact promoted M2a SHA-256 `18e7ed7dea643008eeaa7e0c037708a23e9fcdf0c78d1dcb825aa5bc9a497c56`.

Final x40 qualification:

- Actions run `34431028119`: **completed / success**;
- Chrome `152.0.7977.64`;
- artifact id `10134550393`;
- artifact digest `sha256:08560d219fc82be71305ff85233ab94025212a6ef2d747b08a101e0bd3daef55`.

Machine gates include:

- exact batching parity across seeds `1`, `0x12345678`, `956866913`;
- batch sizes `1 / 7 / 20 / 40 / 120`;
- exact intervention-boundary parity;
- repaired M3 maintained qualification on the composite runtime;
- full M0/M1.1/M2a protected chain;
- repaired M3 real-browser qualification on the composite runtime;
- x40 real-browser speed/debt/pause/visibility/responsiveness/load qualification;
- deterministic standalone build and verify.

### Real-browser x40 results

Target 4x:

- achieved `3.9884x`.

Target 40x, ordinary natural worlds:

- seed `1`: `39.4388x`;
- seed `0x12345678`: `39.6543x`;
- seed `956866913`: `39.7444x`.

All exceed the predeclared ordinary-world gate of **38x**.

At high speed full rendering runs at approximately **9.65-9.99 Hz**, while simulation stepping remains independent.

40x -> 1x transition:

- subsequent achieved speed `0.9983x`;
- final backlog `0.00010 s`.

Thus the old historical-debt failure mode is absent.

Pause:

- exact zero simulation advance while paused.

Hidden tab:

- exact zero hidden simulation advance;
- zero hidden backlog;
- visible resume approximately `39.29x`, without debt repayment.

Control responsiveness specimen:

- event-loop/control lateness `0.9 ms` at 40x.

Heavy-load characterization intentionally demonstrates truthful degradation rather than universal 40x:

- 160 blobs / 300 food: `21.58x`;
- 260 / 300: `13.05x`;
- 260 / 500: `8.10x`.

In those states the UI reports the lower achieved speed, backlog remains below the 0.25 s cap, and the scheduler does not change biology to make the target label true.

See `evidence/x40/X40_MACHINE_QUALIFICATION_REPORT_2026-09-10.md`.

The first x40 workflow run `34430905494` is preserved as a failure. Its x40 causal test passed, but the maintained M3 qualifier rejected a brittle pointer source shape. The M3 qualifier was not weakened; runtime source shape was reconciled and a separate up-to-~100 ms sleeping-pump control delay was removed before the full successful rerun.

## Current Owner gate

There is now exactly **one intended Owner candidate**:

SHA-256:

`21bb4518c3cdf2557ebeb008d9cb9bbfee1ff3e45ca06734d535106db6438b46`

The Owner run must cover:

1. materially narrow/mobile-like complete-world presentation;
2. live wide -> narrow/tall -> wide continuity of the same world;
3. same-seed and new-seed restart after resize;
4. pointer/control sanity where useful;
5. 4x / 10x / 20x / 40x workflow;
6. responsiveness at 40x;
7. plausibility of `cel` versus `realnie`;
8. direct 40x -> 1x transition without historical debt;
9. prompt pause at high speed.

See:

`evidence/x40/X40_COMPOSITE_OWNER_SMOKE_PROTOCOL_2026-09-10.md`

A positive Owner result closes product-feel evidence for this exact composite artifact. Promotion must then occur in provenance/dependency order: repaired M3 first, then x40.

## Current blocking sequence

1. **Do not modify runtime `2081e4a...` without invalidating its machine qualification.**
2. Present exact composite standalone `21bb4518...b46` for Owner smoke.
3. If Owner PASS: record receipt, verify no runtime drift, reconcile live `main`, promote/merge repaired M3 first, then the dependent x40 layer.
4. If Owner finds a material problem: do not merge; reopen only the relevant layer and requalify any changed runtime.

## Cleanup state

- stale README is being reconciled with this authority state;
- obsolete “mobile-like optional” wording in the earlier Owner partial receipt has already been corrected;
- historical failed and superseded qualification receipts remain preserved;
- temporary qualification workflows are removed after evidence capture;
- no frozen V1 or promoted core semantics were rewritten.

## Negative decisions

Do not edit frozen V1. Do not clean up Accidental Biology by inertia. Do not restore viewport-driven world sizing. Do not normalize biology to area. Do not delete explicit `Simulation.setSize()`. Do not replace fixed `1/60` with a large fast-forward timestep. Do not move to a Worker without evidence. Do not optimize core merely to make a 40x label true. Do not promote M2b. Do not claim universal 40x. Do not merge M3 or x40 before the exact composite Owner gate is positive.
